# Agent rules

Only Stage 00 greenfield repository initialization and hosted contract audit are authorized.
Work on `stage/00-initialization` after the initial main baseline is established.
Treat https://github.com/techwithmpg/Cradlehub as read-only. Never modify production, schema, migrations, Auth, RLS, or Storage policies.
Do not inherit historical desktop code or PASS claims. Do not implement product modules, authentication, persistence, caching, or synchronization.
Use one minimal UI direction and no fabricated operational state. Never introduce privileged secrets or renderer command escape hatches.
Push the authorized branch and stop. Do not merge or start Stage 01.
