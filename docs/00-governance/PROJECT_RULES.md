# Project Rules

The complete project rules are maintained at repository root:

`CHATGPT_PROJECT_RULES.md`

This file exists so coding agents have an obvious stable governance path.

If a copy is kept here, it must never drift from the root authoritative version.
